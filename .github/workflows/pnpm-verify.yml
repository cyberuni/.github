name: pnpm-verify
on:
  workflow_call:
    inputs:
      os:
        type: string
        required: false
        default: '["ubuntu-latest", "macos-latest", "windows-latest"]'
      node-version:
        type: string
        required: false
        default: '["lts/-1", "lts/*"]'
      pnpm-version:
        type: string
        required: false
      skip-codecov:
        type: boolean
        required: false
        default: false
      # Which matrix leg uploads coverage. Defaults to `lts/*` so it tracks whatever the
      # current LTS is, rather than naming a Node version that goes stale — a pinned
      # number is what broke this step before (see the codecov step).
      #
      # If a caller's `node-version` matrix contains no `lts/*`, set this to one of the
      # values it does contain, or nothing uploads.
      codecov-node-version:
        type: string
        required: false
        default: 'lts/*'
      skip-playwright:
        type: boolean
        required: false
        default: false
jobs:
  verify:
    runs-on: ${{ matrix.os }}
    strategy:
      # Report every leg. With fail-fast on, the first failure cancels the others and
      # they report "The operation was canceled" with no error of their own — which hides
      # which legs are actually broken and sends you reading the wrong log.
      fail-fast: false
      matrix:
        os: ${{ fromJson(inputs.os) }}
        node-version: ${{ fromJson(inputs.node-version) }}
    steps:
      - uses: actions/checkout@v7
      - uses: cyberuni/.github/.github/actions/setup-node-pnpm@v1
        with:
          node-version: ${{ matrix.node-version }}
          pnpm-version: ${{ inputs.pnpm-version }}

      # Only Linux runners need a virtual display, and only when there are browsers to
      # drive. This previously ran unconditionally, before checkout, on all three OSes —
      # `Xvfb` does not exist on the macOS or Windows images, so those legs were doing
      # nothing useful (they stayed green because the trailing `&` backgrounds it).
      - name: Start X Virtual Frame Buffer
        if: runner.os == 'Linux' && inputs.skip-playwright == false
        run: echo "DISPLAY=:99.0" >> $GITHUB_ENV && Xvfb :99 -screen 0 1920x1080x24 > /dev/null 2>&1 &

      - name: Install Dependencies
        run: pnpm install

      # Browsers come from the composite action, and they are installed AFTER
      # `pnpm install` — both details are load-bearing.
      #
      # This used to be `pnpx playwright install --with-deps chromium`, running BEFORE
      # the install because nothing was in node_modules yet. `pnpx` resolves playwright
      # from the REGISTRY, so it downloaded whatever the latest playwright wanted while
      # the tests ran against the repo's pinned copy — a build-number mismatch, e.g.
      # `chromium_headless_shell-1234` installed against a project expecting `-1208`:
      #
      #   browserType.launch: Executable doesn't exist at
      #   ~/.cache/ms-playwright/chromium_headless_shell-1208/...
      #
      # It fails only on repos that actually drive a browser, so it stayed invisible
      # until one arrived (unional/tersify#443). The composite action runs
      # `pnpm exec playwright install`, which uses the repo's own version, caches on
      # that version rather than on the lockfile hash, and skips gracefully when the
      # repo has no playwright at all. bun-verify.yml has always been shaped this way.
      - name: Install playwright browsers
        if: inputs.skip-playwright == false
        uses: cyberuni/.github/.github/actions/setup-playwright@main

      - name: Verify
        run: pnpm verify

      # Upload once, from the Linux leg running `codecov-node-version` (default `lts/*`).
      #
      # This condition used to read `matrix.node-version == 18`, which could never be
      # true against the default matrix of `["lts/-1", "lts/*"]` — so coverage was
      # silently never uploaded for any repo taking the defaults. Pinning a Node number
      # here is the bug; an `lts/*` alias tracks the matrix instead of going stale.
      #
      # `skip-codecov: true` remains the way to opt out.
      #
      # No CODECOV_TOKEN: uploads rely on the Codecov org setting `Token authentication
      # for public repositories` being `Not required`. Flip that back and every upload
      # fails SILENTLY — Codecov answers `Token required - not valid tokenless upload`
      # and codecov-action reports the rejection as a SUCCESSFUL step. A green codecov
      # step is not proof an upload landed; read the log. Observed on
      # cyberuni/color-map#214.
      #
      # Coverage never gates a merge, so this must stay non-fatal: a Codecov outage must
      # not fail `verify` and with it the required `code / all-checks` context. The badge
      # is the standing monitor — if uploads stop, it goes stale.
      - name: codecov
        if: inputs.skip-codecov == false && matrix.os == 'ubuntu-latest' && matrix.node-version == inputs.codecov-node-version
        uses: codecov/codecov-action@v5

  all-checks:
    needs: [verify]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: All checks passed
        run: |
          if [[ "${{ needs.verify.result }}" != "success" ]]; then
            exit 1
          fi
