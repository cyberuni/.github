name: bun-verify
# cyberuni's bun verify workflow, so a bun repo living here does not have to reach into a
# personal namespace for its `code` job. Counterpart to bun-release-changeset-oidc.yml.
#
# Ported from unional/.github's bun-verify.yml, but follows cyberuni house style rather
# than copying it verbatim:
#   - `skip-codecov` / `skip-playwright` inputs, matching pnpm-verify.yml. unional's
#     version installs playwright browsers unconditionally, which is dead weight for a
#     library with no browser tests.
#   - playwright comes from cyberuni's own composite action, not unional's, so nothing
#     here depends on the personal namespace.
#   - codecov takes an explicit CODECOV_TOKEN, declared under `secrets:` and passed by the
#     caller with `secrets: inherit`. An earlier version of this comment claimed
#     codecov-action is tokenless for public repos; that is false for this org — Codecov
#     answers `Token required - not valid tokenless upload`, and the action reports that
#     rejection as a successful step. See cyberuni/color-map#214.
#
# The `all-checks` job is what produces the required `code / all-checks` context when the
# caller names its job `code`. Do not rename either.
on:
  workflow_call:
    inputs:
      os:
        type: string
        required: false
        default: '["ubuntu-latest", "macos-latest", "windows-latest"]'
      bun-version:
        type: string
        required: false
        default: '["latest"]'
      skip-codecov:
        type: boolean
        required: false
        default: false
      skip-playwright:
        type: boolean
        required: false
        default: false

    secrets:
      CODECOV_TOKEN:
        required: false
        description: >-
          Codecov upload token. Set once as a cyberuni org secret; callers pass
          `secrets: inherit`.

jobs:
  verify:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: ${{ fromJson(inputs.os) }}
        bun-version: ${{ fromJson(inputs.bun-version) }}
    steps:
      - uses: actions/checkout@v7

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ matrix.bun-version }}

      - name: Install Dependencies
        run: bun install

      - name: Install playwright browsers
        if: inputs.skip-playwright == false
        uses: cyberuni/.github/.github/actions/setup-playwright@main
        with:
          package-manager: bun

      - name: Verify
        run: bun run verify

      - name: codecov
        if: inputs.skip-codecov == false && matrix.os == 'ubuntu-latest' && matrix.bun-version == 'latest'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          # Never gate a merge on a coverage service — see pnpm-verify.yml.
          fail_ci_if_error: false

  all-checks:
    needs: [verify]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: All checks passed
        run: |
          if [[ "${{ needs.verify.result }}" != "success" ]]; then
            exit 1
          fi
